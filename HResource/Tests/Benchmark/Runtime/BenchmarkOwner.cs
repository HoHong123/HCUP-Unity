#if UNITY_EDITOR
/* =========================================================
 * @Jason - PKH
 * 벤치마크에서 에셋을 요청하는 소유자 컴포넌트입니다.
 *
 * 특징 / 지원기능 ::
 * 아무 동작도 하지 않습니다. GetAsync 의 소유자 자리에 들어갈 Component 만 제공합니다.
 *
 * 주의사항 ::
 * 라이프사이클 함수를 두지 않습니다. 측정에 소유자 자신의 비용이 섞이지 않게 하기 위해서입니다.
 * =========================================================
 */
#endif

using UnityEngine;

namespace HResource.Benchmark {
    [AddComponentMenu("")]
    public sealed class BenchmarkOwner : MonoBehaviour {
    }
}

#if UNITY_EDITOR
/* =========================================================
 * Dev Log
 * =========================================================
 * 2026-09-23 (최초 설계) :: 빈 소유자 컴포넌트
 *
 * 변경 ::
 * 벤치 전용 빈 MonoBehaviour 를 만들었다.
 *
 * 이유 ::
 * 실제 소비자와 같은 경로(Component 소유자, 프로브 부착)를 타면서 소유자 쪽 비용은 0 이어야 한다.
 * =========================================================
 */
#endif
