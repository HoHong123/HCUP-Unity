#if UNITY_EDITOR
/* =========================================================
 * @Jason - PKH
 * 캐시 한 항목을 어느 소유자가 잡고 있는지 나타내는 진단용 값입니다.
 *
 * 특징 / 지원기능 ::
 * AssetOccupancySnapshot 의 구성 요소. 그 key 를 잡고 있는 소유자 id 하나만 담습니다.
 *
 * 주의사항 ::
 * 에디터 진단 전용이라 통째로 #if UNITY_EDITOR 로 감쌉니다. 빌드에는 들어가지 않습니다.
 * AssetOwnerId 가 아니라 int 를 담습니다. 진단 계층이 런타임 값 타입에 묶이지 않게 합니다.
 * =========================================================
 */

namespace HResource.Cache {
    public readonly struct AssetOwnerOccupancy {
        #region Fields
        public readonly int OwnerId;
        #endregion

        #region 생성자
        public AssetOwnerOccupancy(int ownerId) {
            OwnerId = ownerId;
        }
        #endregion
    }
}

/* =============================================================================
 *  Dev Log
 * =============================================================================
 * =========================================================
 * 2026-09-05 (수정) :: Count 필드 제거
 * =========================================================
 * 변경 ::
 * 소유자별 점유가 횟수에서 유무로 바뀌면서 Count 가 항상 1 이 됐다. 필드를 없앴다.
 *
 * 이유 ::
 * 값이 언제나 같은 필드는 읽는 쪽에 없는 의미를 암시한다. 소유는 불린 관계다.
 *
 * =========================================================
 * @Jason - PKH 2026.09.04 AssetOwnerOccupancy 베이스 코드 생성
 * =========================================================
 *
 * # 목적
 * - Owner Watcher 의 Resource Ownership 패널이 key 를 잡고 있는 소유자 목록을 그리도록 값을 나른다.
 *
 * # 설계 결정
 * - AssetOwnerId 대신 int 를 담는다. 진단 스냅샷은 값의 사본이지 살아있는 참조가 아니며,
 *   에디터 표시에 필요한 것은 숫자뿐이다.
 *
 * =============================================================================
 */
#endif
