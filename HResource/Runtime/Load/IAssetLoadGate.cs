#if UNITY_EDITOR
/* =========================================================
 * @Jason - PKH
 * 동일 key 동시 로드를 합치는 게이트 계약 인터페이스.
 *
 * 주요 기능 ::
 * RunAsync(key, factory) - key 별 비동기 작업을 공유. 같은 key 동시 요청은 한 UniTask 로 dedupe.
 * RunAsync(key, state, factory) - 같은 동작. 호출마다 다른 값을 state 로 넘기면 factory 를 한 번 만들어 재사용할 수 있다.
 *
 * 사용법 ::
 * AssetProvider 가 _GetAsync 에서 source 로드 호출을 본 게이트로 감쌈. 같은 key 가 동시에
 * N 번 요청되어도 source factory 는 1 번만 실행, 나머지 N-1 호출은 진행 중 task 를 await.
 *
 * 주의 ::
 * factory 안에서 같은 key 로 RunAsync 를 다시 부르지 않는다. 기본 구현은 자기 자신에게 합류해 교착한다.
 * 게이트는 진행 중 작업 공유만 책임지고 cache 정책 (저장/만료) 은 다루지 않음 - 책임 분리.
 * =========================================================
 */
#endif

using System;
using Cysharp.Threading.Tasks;

namespace HResource.Load {
    public interface IAssetLoadGate<TKey, TAsset> {
        UniTask<TAsset> RunAsync(TKey key, Func<UniTask<TAsset>> factory);

        /// <summary>
        /// 상태를 인자로 받는 형태. 게이트는 state 를 열어 보지 않고 factory(state) 로 넘기기만 한다
        /// 호출마다 달라지는 값을 람다가 캡처하면 호출마다 클로저와 델리게이트가 생긴다. 이 형태는 그것을 없앤다
        /// </summary>
        UniTask<TAsset> RunAsync<TState>(TKey key, TState state, Func<TState, UniTask<TAsset>> factory);
    }
}

#if UNITY_EDITOR
/* =========================================================
 * Dev Log
 * =========================================================
 * 2026-09-23 (수정 3) :: 168B 와 실측값의 관계 정정
 *
 * 변경 ::
 * 코드 변경 없음. 바로 아래 (수정 2) 가 168B 추정을 실측으로 "바로잡는다" 고 적은 것을 정정한다.
 *
 * 이유 ::
 * 168B 는 Unity 플레이어가 쓰는 Mono 64 비트 기준의 모델이다. 104B / 88B 는 CoreCLR x64 와 Mono 32 비트의 실측이라 런타임이 다르다.
 * 검사관이 크기를 나눠 쟀다. CoreCLR x64 는 클로저 40B + 델리게이트 64B, Mono 32 비트는 24B + 64B 다.
 *
 * 결과 ::
 * 두 값은 서로를 바로잡는 관계가 아니다. Mono 델리게이트를 64 비트로 옮기면 헤더 16B + 포인터 필드 14 개로 128B, 클로저 40B 를 더하면 168B 와 맞는다.
 * (수정 2) 의 수치 행(게이트 경로, 미스 + 반납, 합류자 기울기)은 그대로 맞다.
 *
 * 주의 ::
 * Mono 64 비트는 직접 재지 않았다. 168B 일치도 추정이다.
 *
 * =========================================================
 * 2026-09-23 (수정 2) :: 절감 크기 실측 반영
 *
 * 변경 ::
 * 바로 아래 (수정) 항목의 "요청당 약 168B, 64 비트 추정" 을 실측으로 바로잡는다. 코드는 그대로다.
 *
 * 이유 ::
 * 168B 는 클로저와 델리게이트 몫을 재지 않고 적은 추정이었다. 검사관이 두 런타임에서 적용 전후를 쟀다.
 *
 * 결과 ::
 * 클로저와 델리게이트 몫의 실측은 CoreCLR x64 104B, Mono 32 비트 88B 다(릴리스 코드 생성).
 * 반납이 없는 게이트 경로(SourceOnly 반복)는 요청당 104B -> 0B, 88B -> 0B 가 됐다.
 * 캐시 미스 + 반납 1회 반복은 168B -> 64B, 128B -> 40B 다. 남은 몫은 캐시 항목 생성(획득, CoreCLR 32B / Mono 16B)과 제거 알림(반납, 32B / 24B)이며 적용 전후가 같다.
 * 합류자 1명당 기울기는 138.5B -> 34.5B, 106.5B -> 18.5B 다. Unity Mono 64 비트 플레이어는 재지 않았다.
 *
 * 주의 ::
 * 수치는 검사관 하네스(CoreCLR / Mono 콘솔)에서 잰 값이다. Unity 플레이어의 GC 레코더 수치와 직접 비교하지 않는다.
 *
 * =========================================================
 * 2026-09-23 (수정) :: 상태 인자 형태 추가
 *
 * 변경 ::
 * RunAsync<TState>(key, state, factory) 를 계약에 더했다. 옛 RunAsync(key, factory) 는 남겼다.
 *
 * 이유 ::
 * 호출마다 다른 값을 람다가 캡처하면 호출마다 클로저와 델리게이트가 생긴다(요청당 약 168B, 64 비트 추정).
 * 값을 인자로 넘기면 factory 를 한 번 만들어 재사용할 수 있다.
 *
 * 주의 ::
 * 이 계약의 외부 구현체는 새 메서드를 구현해야 컴파일된다. 저장소 안의 구현체는 SharedAssetLoadGate 하나다.
 * 인터페이스의 제네릭 메서드라 호출이 일반 가상 호출보다 비싸다(Mono 수십 ns 추정, 미측정). IL2CPP 로 옮기면
 * 값 타입 TState 조합의 AOT 생성 여부를 확인해야 한다.
 *
 * =========================================================
 * 2026-09-21 (수정 2) :: 교착을 택한 근거 서술 정정
 *
 * 변경 ::
 * 코드 · 헤더 변경 없음. 아래 항목 "이유" 의 "즉시 멈추는 교착" 서술을 이 항목으로 정정한다.
 *
 * 이유 ::
 * 리뷰 지적. 교착은 예외도 로그도 없이 완료되지 않는 작업으로 조용히 남는다. 에디터 UniTask Tracker 를
 * 켜야만 보이므로 "즉시 멈춘다" 는 틀렸다. 교착을 택한 실제 근거는 추적 비용이다. 교착은 결과를 기다리는
 * 그 호출 한 곳에서 멈춰 원인이 게이트 호출에 모이고, 이중 로드의 refcount 잔존은 한참 뒤 메모리에서만
 * 드러나 원인 지점이 흩어진다.
 *
 * 결과 ::
 * 선택은 그대로다. 근거만 사실에 맞게 바뀌었다.
 *
 * 주의 ::
 * factory 의 동기 구간 재진입은 "지금 factory 를 실행 중인 key" 를 Invoke 전후로 표시해 감지하고
 * InvalidOperationException 으로 바꿀 수 있다. factory 가 await 한 뒤의 재진입은 외부 합류자와 구분할 수
 * 없어 그 방법으로 잡지 못한다. 2026-09-21 기준 호출처가 재진입하지 않아 도입하지 않았다.
 *
 * =========================================================
 * 2026-09-21 (수정) :: 같은 key 재진입을 계약으로 금지
 *
 * 변경 ::
 * 헤더의 "factory 는 동일 key 에 대해 재진입 가능성 고려" 를 "같은 key 로 다시 부르지 않는다" 로 교체.
 *
 * 이유 ::
 * 같은 날 SharedAssetLoadGate 가 factory 호출 전에 key 를 등록하도록 바뀌어, 재진입이 교착이 됐다.
 * 등록을 뒤로 미루면 교착 대신 이중 로드가 되어 Addressables refcount 가 2 로 남는다.
 * 드러나지 않는 잔존보다 즉시 멈추는 교착을 택하고 호출 규칙으로 막는다.
 *
 * 결과 ::
 * 시그니처 변경 없음. 2026-09-21 기준 유일한 호출처 AssetProvider 의 factory 는 게이트를 다시 부르지 않는다.
 *
 * =========================================================
 * 2026-04-26 (수정) :: 헤더 형틀 통합 + Dev Log 형식 도입
 *
 * 변경 ::
 * 기존 헤더 (도입 + 주의사항) 에 "주요 기능 / 사용법" 섹션 추가하여 §11 형틀 통일.
 * 하단 Dev Log 영역 추가. 헤더와 Dev Log 모두 #if UNITY_EDITOR 가드.
 *
 * 이유 ::
 * 글로벌 CLAUDE.md §11 룰 일괄 적용.
 *
 * =========================================================
 * 2026-04-25 (최초 설계) :: IAssetLoadGate 초기 구현
 *
 * 동일 key 동시 로드 dedupe 라는 단일 책임만 노출. 결과 캐시 (cache 정책) 와 명확히 분리.
 * 기본 구현체 SharedAssetLoadGate 가 17 줄짜리 finally cleanup 패턴으로 진행 중 task 공유.
 * 게이트 자체는 source 종류와 무관한 공통 도구 - Resources/Addressable 어느 loader 에도 적용.
 * =========================================================
 */
#endif
