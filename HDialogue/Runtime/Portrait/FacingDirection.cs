#if UNITY_EDITOR
/* =========================================================
 * @Jason - PKH
 * -- 캐릭터 포트레이트 방향 열거형.
 *
 * 특징 / 지원기능 ::
 * + Left  - 캐릭터가 오른쪽을 향함 (원본 스프라이트 기준)
 * + Right - 캐릭터가 왼쪽을 향함 (X 반전)
 *
 * 주의사항 ::
 * "방향" 전용 — 슬롯 위치 식별자는 StageSlot 사용.
 * A안 플립 구현: CharacterPortraitController.SetFacing에서 localScale.x *= -1.
 * =========================================================
 */
#endif

namespace HDialogue {
    public enum FacingDirection {
        Left,
        Right,
    }
}

#if UNITY_EDITOR
/* =============================================================================
 *  Dev Log
 * =============================================================================
 * @Jason - PKH 2026.05.17 (수정) :: 헤더 — 슬롯 위치 혼용 설명 제거
 *
 * # 변경
 * - 슬롯 위치 식별자 역할 설명 제거 → StageSlot으로 완전 이관.
 * - "방향 전용 — 슬롯 위치는 StageSlot" 주의사항 명시.
 *
 * =============================================================================
 * @Jason - PKH 2026.05.15 FacingDirection 베이스 코드 생성
 *
 * # 목적
 * - 포트레이트 방향 표현 — Phase 4 CharacterPortraitController.SetFacing 에 사용
 * - DialogueLineNode.SpeakerFacing 필드에서 Phase 1 시점 선행 참조
 *
 * =============================================================================
 */
#endif
