#if UNITY_EDITOR
/* =========================================================
 * @Jason - PKH
 * -- 배포 로그 심각도 열거형입니다.
 * =========================================================
 */
#endif

namespace HDeploy.Deploy {
    public enum DeployLogSeverity {
        Info,
        Warning,
        Error,
    }
}

#if UNITY_EDITOR
/* =============================================================================
 *  Dev Log
 * =============================================================================
 * @Jason - PKH 2026.07.03 Deploy 디렉토리 재배치
 *
 * # 코드 리펙토링
 * - Editor 루트 → Editor/Deploy/ 이동, namespace HDeploy → HDeploy.Deploy (디렉토리 기반 네임스페이스 컨벤션).
 *
 * =============================================================================
 * @Jason - PKH 2026.07.03 DeployLogSeverity 베이스 코드 생성
 *
 * # 목적
 * - DeployLog 엔트리 심각도 분류. (DeployLog 중첩 타입에서 파일 분리 — 컨벤션 준수)
 *
 * =============================================================================
 */
#endif
