#if UNITY_EDITOR
/* =========================================================
 * @Jason - PKH
 * RecycleView Cell 데이터의 베이스 추상 클래스. 마커 베이스로만 사용.
 *
 * 주요 기능 ::
 * 빈 추상 클래스 — RecycleView 의 TCellData 제약을 위한 type marker.
 *
 * 사용법 ::
 * 도메인 데이터 모델이 본 클래스를 상속하지 않아도 됨 — TCellData 의 generic 제약은 `class` 만
 * 요구. 본 클래스는 "RecycleView Cell 데이터" 의미를 명시적으로 표현하고 싶을 때 옵션으로 사용.
 *
 * 주의 ::
 * 빈 클래스라 추가 멤버 도입 시 모든 도메인 모델에 영향. 멤버 추가 전에 RecycleView 사용처
 * 전수 검토 필요.
 * =========================================================
 */
#endif

namespace HUI.ScrollView {
    public abstract class BaseRecycleCellData {}
}

#if UNITY_EDITOR
/* =========================================================
 * Dev Log
 * =========================================================
 *
 * =========================================================
 * 2026-04-26 (수정) :: 헤더 형틀 신규 + Dev Log 형식 도입
 * =========================================================
 * 변경 ::
 * 기존 헤더 주석 부재 → §11 형틀 헤더 신규 작성 (도입 / 주요 기능 / 사용법 / 주의 4 섹션).
 * 하단 Dev Log 영역 추가. 헤더와 Dev Log 모두 #if UNITY_EDITOR 가드.
 *
 * 이유 ::
 * 글로벌 CLAUDE.md §11 룰 일괄 적용. 빈 클래스라도 의도 (마커 베이스) 를 헤더에 명시.
 *
 * =========================================================
 * 2026-04-25 (최초 설계) :: BaseRecycleCellData 초기 구현
 * =========================================================
 * 도메인 데이터 모델의 marker 역할. 의도적으로 비워둠 — 멤버 추가 시 모든 도메인 모델에
 * 강제 영향이 가므로 빈 클래스로 출발하여 보수적으로 확장. RecycleView 의 TCellData generic
 * 제약은 `class` 만 요구하므로 본 베이스 상속은 의미 표현용 옵션.
 * =========================================================
 */
#endif
