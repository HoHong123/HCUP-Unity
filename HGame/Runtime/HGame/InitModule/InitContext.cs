#if UNITY_EDITOR
/* =========================================================
 * @Jason - PKH
 * 페이즈 훅에 공유되는 초기화 컨텍스트 클래스입니다.
 * =========================================================
 */
#endif

namespace HGame.Flow {
    public sealed class InitContext {
        public float TimeScale = 1f;
    }
}

#if UNITY_EDITOR
/* =============================================================================
 *  Dev Log
 * =============================================================================
 * @Jason - PKH 2026.08.04 GameContext 를 InitContext 로 개칭
 *
 * # 변경
 * - 클래스명 GameContext -> InitContext, 파일명 동기화 (guid 보존).
 *
 * # 이유
 * - InitModule 폴더 내 스크립트의 Game 접두를 Init 으로 통일 (Phase 1 HCUP 정리 후속).
 *
 * =============================================================================
 */
#endif
